[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "accelerate>=1.13.0",
    "huggingface-hub>=1.3.7",
    "numpy>=2.4.2",
    "pydantic>=2.12.5",
    "torch>=2.10.0",
    "transformers>=5.0.0",
]
